<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodassessment">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:noHistory="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".FoodActivity" />
        <activity android:name=".FoodCollectionsActivity" />
        <activity android:name=".AddFoodActivity" />
        <activity
            android:name=".CalendarActivity"
            android:label="غذاؤنا" />
        <activity
            android:name=".StartOrHelpActivity"
            android:label="غذاؤنا" />
        <activity
            android:name=".SignUpActivity"
            android:label="غذاؤنا" />
        <activity
            android:name=".excel.ExportToExcelActivity"
            android:label="غذاؤنا" />
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>